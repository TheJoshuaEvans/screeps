/**
 * Simple module holds string constants for different states
 */
module.exports = {
  /**
   * The "BUILD" state is used by builders to construct structures
   */
  BUILD: 'BUILD',

  /**
   * The "DELIVER" state is used to deliver energy to structures that require it
   */
  DELIVER: 'DELIVER',

  /**
   * The "GATHER" state is used to gather energy from sources
   */
  GATHER: 'GATHER'
}
